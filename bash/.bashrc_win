#! /usr/bin/env bash

source ~/.bashrc_base

# set X11 display
export DISPLAY=localhost:0.0

# enable opengl acceleration for vcXsrv
export LIBGL_ALWAYS_INDIRECT=1

# add custom programs to path
MYPROG="${HOME}/programs"
export PATH="${HOME}/bin:${MYPROG}/anaconda2/bin:$PATH"

